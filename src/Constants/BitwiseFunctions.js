const BITWISE_FUNCTIONS = {
  'XOR': (a, b) => a ^ b,
  'OR': (a, b) => a | b,
  'AND': (a, b) => a & b,
  'Signed right shift': (a,b) => a >> b,
  'Zeros fill right shift': (a,b) => a >>> b
}

export default BITWISE_FUNCTIONS